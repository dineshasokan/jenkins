pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
       powershell 'docker pull docker/getting-started'
       powershell 'docker run -d  docker/getting-started'
      }
    }
  }
}
